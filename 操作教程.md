# 不写代码，铺一条自然的小路

配套 Godot 4.7.2，Compatibility。2D 像素等轴视角；地面网格 **64×32**，PNG 切片 **64×64**。工程内没有脚本、着色器或插件。视频长 6 分钟，重复配置可以暂停对照本说明完成。

## 怎么开始

- **直接体验**：在 Godot 项目管理器导入 `complete/project.godot`，打开 `main.tscn`。选 Grass → 下方 TileMap → 地形 → 草地 → 连接图标。左键铺、右键擦。换 Path → 泥路，编辑上层小路。F6 查看场景。
- **跟着从素材做**：导入 `starter/project.godot`。这里只有保存好的空 Node2D 场景和三张 PNG，窗口与像素过滤已设好；下面的 TileSet 和图层都留给你配置。
- **完全从空项目做**：新建 Compatibility 项目，把 starter/assets 复制到新项目；照以下步骤完成。需要跳过重复标记时，把 complete 的 `ground_tileset.tres` 复制过来，保留相同 assets 路径。

## 1. 窗口和像素

项目 → 项目设置，搜索 `viewport`：视口宽度 640、高度 360。搜索 `default_texture_filter`，渲染 → 纹理 → 默认纹理过滤选 **Nearest**。窗口拉伸模式设为 `canvas_items`；完成工程用 1280×720 窗口，画面按整数倍放大。

## 2. 节点

点击“二维场景”创建 Node2D，重命名 Ground，Ctrl+S 保存 main.tscn。在左侧“+”或 Ctrl+A 搜索并添加 **TileMapLayer** 两次，分别命名 Grass、Path。两个都是 Ground 的直接子节点，Path 排在 Grass 后面，保持同样的位置与缩放。

## 3. TileSet 与 PNG

1. 选 Grass，在检查器的 Tile Set 下拉里选 **新建 TileSet**，点击资源展开。
2. Tile Shape = Isometric；Tile Layout = Diamond Down；Tile Size = (64,32)。根节点保持默认缩放。
3. 底部切到 TileSet，把 `grass.png` 从文件系统拖入图块源列表。把 **Texture Region Size** 改为 (64,64)。自动创建提示可以先拒绝，设置好切片后，在“设置”模式用鼠标框出有效格子。
4. grass/path 图集都是 8 列、8 行：前 6 行是基础形态，第 6 行只用前 7 格，共 47 格；坐标 (7,5) 的空白不要创建。最后两行是 16 个同规则的外观变化，也要创建。共 63 格。若曾按默认 16×16 自动创建，请先清掉错误图块再按 64×64 切。
5. `path.png` 同样添加。`grass_variants.png` 添加后**只创建后两格**：坐标 (1,0) 小草、(2,0) 石头；第一格仅作普通地面参照，不使用。

网格尺寸决定摆放位置，切片尺寸决定从 PNG 取多大一块。64×64 的图片里放的是 64×32 的菱形表面，以及透明留白、少量底边厚度。两组数字不同是有意的。

## 4. 地形连接规则

1. 展开 TileSet 的 **Terrain Sets**，添加两组，每组 Mode 都选 **Match Corners and Sides**。
2. 在第 0 组的 Terrains 里添加一个地形，命名“草地”；第 1 组里添加“泥路”。颜色任意，绿色和土黄色更便于识别；这只影响编辑标记。
3. 选草地图集 → **绘制** → 属性下拉“地形” → Terrain Set 0 → Terrain 草地。先给每块的中心指定草地，再标连接区域。
4. 全内部图块 (0,0)：中心、四边、四角全部标草地。外边缘：朝外的区域保留“没有地形”（-1），其他相接方向标草地。左键涂，右键清除。
5. 打开旁边的 **地形标记对照.svg**，按图集坐标完成基础 47 块。最后两行 16 个外观变化，照图表末尾对应的基础格复制地形标记，Probability 设为 1。小圆点是方向示意，Godot 中对应的是菱形内的区域。别漏掉内凹角、孤立块、细路和端点；缺图块时工具只能找近似结果。
6. 泥路按同一张对照图标记，但选 Terrain Set 1 → 泥路。**朝外仍然是没有地形，不是草地**；路的透明边缘负责露出下层草地。
7. 用资源保存菜单保存为 `ground_tileset.tres`。再选 Path，把同一个资源拖进 Tile Set 槽。

## 5. 实际铺设

选 Grass → 底部 **TileMap** → **地形** → 草地 → **连接 / Connect** 图标（四向箭头图标）。左键拖动铺一片约 7×5 格的草地，外沿少铺几格，留出宽窄变化。这个模式会同时重新匹配邻居。

接着选 Path → 泥路 → 连接，画一段、转弯、补宽一格，再用右键擦掉一格。上层被擦掉后露出下层草地，路沿会重新接好。使用普通“图块”印章直接盖单块，邻居不会按这个流程自动接边。

**两层各管各的**：擦 Grass 不会自动擦 Path；笔刷也不会阻止你把路铺到草地外。让路留在草地内部。游戏运行后只是展示，鼠标绘图是编辑器功能，并没有制作运行时地图编辑器。

## 6. 小草和石头

这次使用的装饰已经画在 PNG 里。给草地变体 (1,0)、(2,0) 设置与完整内部格相同的 9 个地形标记。在 TileSet 的“选择”模式点图块，展开 **杂项 → 概率（Probability）**：

| 候选图块 | Probability |
| --- | ---: |
| 普通完整草地：grass.png (0,0) | 8 |
| 三种完整草地纹理：grass.png (0,6)、(1,6)、(2,6) | 各 1 |
| 小草：grass_variants.png (1,0) | 1.4 |
| 石头：grass_variants.png (2,0) | 0.65 |

它们只在连接规则相同的内部格中作为候选；数值是相对权重，不是百分比。每次铺设可能不同，不保证数量、间距，也不是独立撒物系统。草地上的细节可能被上面的泥路遮住。完成场景保留了少量相同规则的变体，便于看清示范。

## 7. 镜头、运行和保存

在 Ground 下添加 Camera2D，Zoom = (1,1)，Position = (0,16)。F6 运行当前场景，或设 main 为主场景后 F5。Camera2D 只改变观察范围；等轴视角来自图块和网格。

Ctrl+S 保存场景，确认 TileSet 的外部资源也已保存。关闭 main.tscn 再打开，检查图块仍在、还能继续铺设。工程不包含碰撞和人物，本节只做地面显示。

## 接不上时

先核对：两个尺寸是否填反；地形组有没有选错；中心和边角是否漏标；对应外角/内角/孤立图块是否存在；是否用了“地形 → 连接”笔刷。像素发软时检查 Nearest，并优先使用整数倍预览。

## 官方资料

- [Godot：使用 TileSet](https://docs.godotengine.org/en/stable/tutorials/2d/using_tilesets.html)
- [Godot：使用 TileMap](https://docs.godotengine.org/en/stable/tutorials/2d/using_tilemaps.html)

素材来源和制作端辅助工具说明见 ASSET_NOTICE.md。
